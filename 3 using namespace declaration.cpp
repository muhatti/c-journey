#include <iostream> // header
using namespace std; /* using namespace is a declaration that enebles one to 
use IO objects without one 
specifying the scope in every line*/

int main() // main function
{
	cout << "Enter any number:"; 
	int n1=0;
	cin >> n1;
    cout << " The number entered is " << n1;
	return 0;
} 